import React from 'react';
import SeatSelection from './SeatSelection';

const SeatSelectionContainer = () => {
  return (
    <div>
      <SeatSelection/>
    </div>
  );
};

export default SeatSelectionContainer;