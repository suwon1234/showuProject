// 제보내역 스타일4

import styled  from "styled-components";
import { backgroundBlack } from "../../../global/common";

const S = {};
    S.Wrapper = styled.div` // 전체 배경 black
        ${backgroundBlack}     
    `;

export default S;